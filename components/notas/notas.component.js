import template from './notas.html';
import controller from './notas.controller';
import './notas.scss';

let notasComponent = {
  bindings: {},
  template,
  controller
};

export default notasComponent;
